self.addEventListener('push',event=>{let d={title:'イコエル相談チャット',body:'新しい通知があります',url:'/consult'};try{if(event.data)d={...d,...event.data.json()}}catch{}event.waitUntil(self.registration.showNotification(d.title,{body:d.body,tag:d.type||'consult',renotify:true,requireInteraction:!!d.requireInteraction,data:{url:d.url||'/consult'}}))});self.addEventListener('notificationclick',e=>{e.notification.close();const url=e.notification.data?.url||'/consult';e.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(ws=>ws[0]?ws[0].focus().then(()=>ws[0].navigate(url)):clients.openWindow(url)))});
